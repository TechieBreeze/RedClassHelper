---
phase: 02-desktop-file-import-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/features/import/providers/import_notifier.dart
  - lib/features/import/providers/import_state.dart
  - lib/features/import/presentation/import_preview_screen.dart
  - lib/features/import/presentation/import_summary_screen.dart
  - lib/routing/router.dart
autonomous: true
gap_closure: true
requirements:
  - IMP-04
  - QST-03
  - UI-04

must_haves:
  truths:
    - "用户可在预览页编辑题库名称，含 CJK 感知的 20 字符限制（D-18）"
    - "导入总结页展示因警告跳过的题目列表及逐条重试/手动编辑按钮（D-09）"
    - "直接访问过期 /import/preview/:jobId 或 /import/summary/:jobId 时重定向到首页"
  artifacts:
    - path: "lib/features/import/presentation/import_preview_screen.dart"
      provides: "题库名称 TextField + CJK 字符计数 + jobId 守卫"
      contains: "_bankNameController"
    - path: "lib/features/import/presentation/import_summary_screen.dart"
      provides: "跳过题目列表区域 + 重试/手动编辑按钮"
      contains: "skippedCandidates"
    - path: "lib/routing/router.dart"
      provides: "导入路由 redirect 守卫"
      contains: "redirect:"
    - path: "lib/features/import/providers/import_notifier.dart"
      provides: "setBankName() 方法"
      contains: "void setBankName"
    - path: "lib/features/import/providers/import_state.dart"
      provides: "skippedCandidates getter"
      contains: "skippedCandidates"
  key_links:
    - from: "import_preview_screen.dart bankName TextField"
      to: "ImportNotifier.setBankName()"
      via: "onChanged callback"
      pattern: "setBankName"
    - from: "import_summary_screen.dart 重试按钮"
      to: "ImportNotifier.retryParseCandidate()"
      via: "onPressed callback"
      pattern: "retryParseCandidate"
    - from: "router.dart redirect"
      to: "ImportNotifier (importNotifierProvider)"
      via: "ProviderScope.containerOf"
      pattern: "containerOf|redirect"
---

<objective>
补全导入预览页和摘要页的缺失功能：题库名称编辑（D-18 CJK 字符限制）、过期 jobId 路由守卫、摘要页跳过题目列表含重试/手动编辑按钮（D-09）。

Purpose: VERIFICATION.md Gap #2（题库名称编辑）、Gap #3（摘要跳过项）、Gap #4（路由守卫）——预览编辑缺失核心 UX，摘要页遗漏 Plan 要求的跳过项 UI，深链接到过期 jobId 缺乏防护。
Output: 预览页顶部含题库名称 TextField（CJK 感知上限）、摘要页展示跳过题目并支持逐条重试/手动编辑、GoRouter redirect 守卫阻止过期路由访问。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-desktop-file-import-pipeline/02-VERIFICATION.md
@.planning/phases/02-desktop-file-import-pipeline/02-00-SUMMARY.md
@.planning/phases/02-desktop-file-import-pipeline/02-CONTEXT.md D-18（题库名称 CJK 字符限制）
@.planning/phases/02-desktop-file-import-pipeline/02-00-PLAN.md Task "task-preview-screen"（原始需求规格）
@.planning/phases/02-desktop-file-import-pipeline/02-00-PLAN.md Task "task-summary-screen"（原始需求规格）
@.planning/phases/02-desktop-file-import-pipeline/02-00-PLAN.md Task "task-routing-updates"（原始路由守卫需求）
</context>

<interfaces>
<!-- 现有接口——executor 直接使用，无需自行探索代码库 -->
<!-- 从 lib/features/import/providers/import_state.dart 提取 -->
```dart
class ImportState {
  final String jobId;
  final ImportPhase phase;
  final List<ImportFile> files;
  final String extractedText;
  final List<ParseCandidate> candidates;
  final Set<int> confirmedIndices;
  final double progress;
  final String? error;
  final int committedCount;
  final String bankName;
  // getters: isIdle, isExtracting, isParsing, isEditing, isCommitting, isDone, hasError, hasCandidates
  // method: copyWith({jobId, phase, files, extractedText, candidates, confirmedIndices, progress, error, committedCount, bankName, clearError})
}
```

<!-- 从 lib/features/import/parsing/parse_candidate.dart 提取 -->
```dart
enum CandidateType { singleChoice, multiChoice, trueFalse, shortAnswer, unknown }

class ParseCandidate {
  final String rawText;         // 原始文本块——重试解析的关键数据
  final CandidateType candidateType;
  final String title;
  final List<String> options;
  final String answer;
  final String explanation;
  final double confidence;      // 0.0–1.0，用于推导跳过原因
  final int startLine;
  final int endLine;
  final Map<String, String> metadata;
  // copyWith(...), toJson(), fromJson()
}
```

<!-- 从 lib/features/import/providers/import_notifier.dart 提取（修改前） -->
```dart
// ImportNotifier 现有方法：
// - pickFiles(List<ImportFile> files)
// - extractAndParse()
// - toggleCandidate(int index)
// - setCandidateType(int index, CandidateType type)
// - setCandidateOptions(int index, List<String> options)
// - setCandidateAnswer(int index, String answer)
// - commitToDatabase()
// - reset()
```

<!-- 从 lib/routing/router.dart 提取 -->
```dart
final GoRouter appRouter = GoRouter(
  initialLocation: '/',
  routes: [
    GoRoute(path: '/', builder: ...),
    GoRoute(path: '/import', builder: ...),
    GoRoute(path: '/import/progress', builder: ...),
    GoRoute(path: '/import/preview/:jobId', builder: ...),   // 需加 redirect
    GoRoute(path: '/import/summary/:jobId', builder: ...),   // 需加 redirect
  ],
);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ImportState 添加 skippedCandidates getter + ImportNotifier 添加 setBankName() 和 retryParseCandidate()</name>
  <files>lib/features/import/providers/import_state.dart, lib/features/import/providers/import_notifier.dart</files>

  <read_first>
    - lib/features/import/providers/import_state.dart — ImportState 类定义（当前无 skippedCandidates）
    - lib/features/import/providers/import_notifier.dart — ImportNotifier 类定义（当前无 setBankName/retryParseCandidate）
    - lib/features/import/parsing/parse_candidate.dart — ParseCandidate 字段（rawText, confidence, candidateType 等）
  </read_first>

  <action>
    1. **在 ImportState 中添加 `skippedCandidates` getter**（import_state.dart，ImportState 类体内，`hasCandidates` getter 之后）：
       ```dart
       /// 未被用户确认的候选题目（即跳过项），带跳过原因
       List<({int index, ParseCandidate candidate, String reason})> get skippedCandidates {
         return candidates.asMap().entries
             .where((e) => !confirmedIndices.contains(e.key))
             .map((e) {
               final c = e.value;
               final reason = _deriveSkipReason(c);
               return (index: e.key, candidate: c, reason: reason);
             })
             .toList();
       }

       /// 推导跳过原因
       String _deriveSkipReason(ParseCandidate c) {
         if (c.confidence < 0.3) return '置信度过低';
         if (c.candidateType == CandidateType.unknown) return '题型未识别';
         if (c.title.isEmpty && c.options.isEmpty) return '题干和选项均缺失';
         if (c.options.length < 2) return '选项不足（少于2个）';
         if (c.answer.isEmpty) return '答案未识别';
         return '用户跳过';
       }
       ```
       注意：`_deriveSkipReason` 需要 import ParseCandidate 和 CandidateType——这些已在现有 import 中。

    2. **在 ImportNotifier 中添加 `setBankName` 方法**（import_notifier.dart，`_deriveBankName` 方法之后）：
       ```dart
       /// 设置题库名称（用户在预览页编辑时调用）
       void setBankName(String name) {
         state = state.copyWith(bankName: name);
       }
       ```

    3. **在 ImportNotifier 中添加 `retryParseCandidate` 方法**（import_notifier.dart，`setCandidateAnswer` 方法之后）：
       ```dart
       /// 重新解析单个候选题目（摘要页"重试"按钮触发）
       void retryParseCandidate(int index) {
         if (index < 0 || index >= state.candidates.length) return;

         final rawText = state.candidates[index].rawText;
         final reparsed = _parser.parse(rawText, bankName: state.bankName);

         final candidates = List<ParseCandidate>.from(state.candidates);
         if (reparsed.isNotEmpty) {
           // 保留原始行号信息
           candidates[index] = reparsed.first.copyWith(
             startLine: candidates[index].startLine,
             endLine: candidates[index].endLine,
           );
         }
         // 无论重解析成功与否，都加入确认集
         final confirmed = Set<int>.from(state.confirmedIndices);
         confirmed.add(index);
         state = state.copyWith(candidates: candidates, confirmedIndices: confirmed);
       }
       ```

    4. **确保新方法被 .g.dart 正确生成**——Riverpod 注解类（@riverpod）的 `.g.dart` 通过 build_runner 生成。新添加的方法如果是 public 的且属于 Notifier 类，build_runner 会自动包含它们。Plan 02-01 已运行 build_runner。
  </action>

  <verify>
    <automated>grep -n "skippedCandidates" lib/features/import/providers/import_state.dart && grep -n "void setBankName" lib/features/import/providers/import_notifier.dart && grep -n "void retryParseCandidate" lib/features/import/providers/import_notifier.dart</automated>
  </verify>

  <acceptance_criteria>
    - `ImportState` 含有 getter `skippedCandidates`，返回 `List<({int index, ParseCandidate candidate, String reason})>`
    - `skippedCandidates` 仅包含未被确认的候选（`!confirmedIndices.contains`）
    - 跳过原因推导规则：confidence < 0.3 → '置信度过低'；unknown → '题型未识别'；空题干选项 → '题干和选项均缺失'；少于2选项 → '选项不足（少于2个）'；空答案 → '答案未识别'；否则 '用户跳过'
    - `ImportNotifier` 含有方法 `void setBankName(String name)` —— 调用 `state.copyWith(bankName: name)`
    - `ImportNotifier` 含有方法 `void retryParseCandidate(int index)` —— 对 `candidates[index].rawText` 重新调用 `_parser.parse()`，更新 candidates 列表并加入 confirmedIndices
    - `flutter analyze lib/features/import/providers/` 无错误
  </acceptance_criteria>

  <done>ImportState 可计算跳过项列表含原因；ImportNotifier 支持编辑题库名称和逐条重解析。</done>
</task>

<task type="auto">
  <name>Task 2: ImportPreviewScreen 添加题库名称 TextField（D-18 CJK）+ jobId 有效性守卫</name>
  <files>lib/features/import/presentation/import_preview_screen.dart</files>

  <read_first>
    - lib/features/import/presentation/import_preview_screen.dart — 当前布局：AppBar(title) → _buildToolbar → ListView.builder
    - lib/features/import/providers/import_state.dart — ImportState.bankName 字段
    - lib/features/import/providers/import_notifier.dart — setBankName() 方法（Task 1 添加）
    - .planning/phases/02-desktop-file-import-pipeline/02-CONTEXT.md D-18
  </read_first>

  <action>
    1. **在 _ImportPreviewScreenState 中添加 TextEditingController**：
       ```dart
       late final TextEditingController _bankNameController;
       String? _bankNameError;
       
       @override
       void initState() {
         super.initState();
         // 从 state 初始化 controller
         final bankName = ref.read(importNotifierProvider).bankName;
         _bankNameController = TextEditingController(text: bankName);
         
         // jobId 有效性守卫
         WidgetsBinding.instance.addPostFrameCallback((_) {
           final state = ref.read(importNotifierProvider);
           if (!state.isEditing && !state.isCommitting) {
             if (mounted) context.go('/');
           }
         });
       }
       
       @override
       void dispose() {
         _bankNameController.dispose();
         super.dispose();
       }
       ```

    2. **CJK 感知字符计数函数**（添加到 `_ImportPreviewScreenState` 中）：
       ```dart
       /// 计算字符串的 CJK 感知长度：中文/全角字符=2，ASCII=1
       int _cjkAwareLength(String text) {
         int len = 0;
         for (final char in text.characters) {
           final code = char.codeUnitAt(0);
           // CJK统一表意文字 (U+4E00–U+9FFF)、全角字母/数字 (U+FF01–U+FF5E)、
           // 中文标点 (U+3000–U+303F)、全角片假名等
           if ((code >= 0x4E00 && code <= 0x9FFF) ||
               (code >= 0xFF01 && code <= 0xFF5E) ||
               (code >= 0x3000 && code <= 0x303F) ||
               (code >= 0x3400 && code <= 0x4DBF) ||
               (code >= 0xF900 && code <= 0xFAFF)) {
             len += 2;
           } else {
             len += 1;
           }
         }
         return len;
       }
       
       /// 验证题库名称，返回 null 表示有效
       String? _validateBankName(String name) {
         final trimmed = name.trim();
         if (trimmed.isEmpty) return '请输入题库名称';
         if (_cjkAwareLength(trimmed) > 20) return '题库名称过长（上限20字符，中文=2）';
         return null;
       }
       ```

    3. **在 Scaffold body 顶部插入题库名称编辑区**。
       修改 body 的 Column children，在 `_buildToolbar` **之前**插入：
       ```dart
       // 题库名称编辑区（在 _buildToolbar 之前）
       Padding(
         padding: const EdgeInsets.fromLTRB(16, 12, 16, 4),
         child: TextField(
           controller: _bankNameController,
           decoration: InputDecoration(
             labelText: '题库名称',
             hintText: '输入题库名称',
             errorText: _bankNameError,
             helperText: '中文/全角=2字符，ASCII=1字符，上限20',
             border: const OutlineInputBorder(),
             isDense: true,
           ),
           onChanged: (value) {
             setState(() => _bankNameError = _validateBankName(value));
             if (_bankNameError == null) {
               ref.read(importNotifierProvider.notifier).setBankName(value.trim());
             }
           },
         ),
       ),
       ```
       body 的 Column 结构变为：
       ```
       Column(
         children: [
           Padding(/* 题库名称 TextField */),    // ← 新增
           _buildToolbar(...),                    // 现有
           Expanded(ListView.builder(...)),       // 现有
         ],
       )
       ```

    4. **更新 `_onSave` 方法**——提交前验证题库名称：
       ```dart
       Future<void> _onSave(BuildContext context) async {
         // 验证题库名称
         final nameError = _validateBankName(_bankNameController.text);
         if (nameError != null) {
           setState(() => _bankNameError = nameError);
           return;
         }
         // 确保 state 中的 bankName 是最新的
         ref.read(importNotifierProvider.notifier).setBankName(_bankNameController.text.trim());
         
         final notifier = ref.read(importNotifierProvider.notifier);
         await notifier.commitToDatabase();
         final state = ref.read(importNotifierProvider);
         if (state.isDone && mounted) {
           context.go('/import/summary/${state.jobId}');
         }
       }
       ```
  </action>

  <verify>
    <automated>grep -n "_bankNameController" lib/features/import/presentation/import_preview_screen.dart && grep -n "cjkAwareLength\|_validateBankName" lib/features/import/presentation/import_preview_screen.dart && grep -n "jobId\|initState" lib/features/import/presentation/import_preview_screen.dart | head -5</automated>
  </verify>

  <acceptance_criteria>
    - `ImportPreviewScreen` 顶部包含一个 `TextField`，标签为 '题库名称'，从 `state.bankName` 预填充
    - `_cjkAwareLength()` 函数：中文字符（U+4E00–U+9FFF）计为2，ASCII 计为1
    - 上限：CJK 感知长度 > 20 → 显示红色错误 '题库名称过长（上限20字符，中文=2）'
    - 空名称 → 显示红色错误 '请输入题库名称'
    - TextField.onChanged 调用 `setBankName()` 同步到 ImportNotifier
    - `initState` 中有 jobId 有效性守卫：`!state.isEditing && !state.isCommitting` → `context.go('/')`
    - `_onSave` 提交前验证名称有效性，无效时中止提交
    - `flutter analyze lib/features/import/presentation/import_preview_screen.dart` 无错误
  </acceptance_criteria>

  <done>预览页顶部含可编辑题库名称输入框，CJK 感知的 20 字符限制实时验证，空名阻止提交；过期 jobId 自动重定向到首页。</done>
</task>

<task type="auto">
  <name>Task 3: ImportSummaryScreen 添加跳过题目列表（D-09）+ GoRouter redirect 守卫</name>
  <files>lib/features/import/presentation/import_summary_screen.dart, lib/routing/router.dart</files>

  <read_first>
    - lib/features/import/presentation/import_summary_screen.dart — 当前仅有成功统计和题型分布
    - lib/routing/router.dart — 当前 /import/preview/:jobId 和 /import/summary/:jobId 无 redirect
    - lib/features/import/providers/import_state.dart — skippedCandidates getter（Task 1 添加）
    - lib/features/import/providers/import_notifier.dart — retryParseCandidate() 方法（Task 1 添加）
  </read_first>

  <action>
    1. **修改 ImportSummaryScreen** —— 在题型分布卡片之后、CTA 按钮之前，插入跳过题目列表区域：

       在 `_buildTypeBreakdown` 调用之后（约第117行 `const SizedBox(height: 32)` 之前），插入：
       ```dart
       // ── 跳过题目列表（D-09）──
       if (state.skippedCandidates.isNotEmpty) ...[
         const SizedBox(height: 24),
         Card(
           color: Colors.orange.shade50,
           child: Padding(
             padding: const EdgeInsets.all(16),
             child: Column(
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
                 Row(
                   children: [
                     Icon(Icons.warning_amber_rounded, 
                          color: Colors.orange.shade700, size: 20),
                     const SizedBox(width: 8),
                     Text(
                       '⚠ 跳过 ${state.skippedCandidates.length} 题',
                       style: theme.textTheme.titleSmall?.copyWith(
                         color: Colors.orange.shade900,
                         fontWeight: FontWeight.w600,
                       ),
                     ),
                   ],
                 ),
                 const SizedBox(height: 12),
                 ...state.skippedCandidates.map((item) {
                   final displayIndex = item.index + 1; // 人类可读编号从1开始
                   return Padding(
                     padding: const EdgeInsets.only(bottom: 8),
                     child: Row(
                       crossAxisAlignment: CrossAxisAlignment.start,
                       children: [
                         Expanded(
                           child: Column(
                             crossAxisAlignment: CrossAxisAlignment.start,
                             children: [
                               Text(
                                 '#$displayIndex: ${item.reason}',
                                 style: theme.textTheme.bodyMedium?.copyWith(
                                   color: Colors.orange.shade900,
                                 ),
                               ),
                               if (item.candidate.title.isNotEmpty)
                                 Text(
                                   item.candidate.title,
                                   maxLines: 1,
                                   overflow: TextOverflow.ellipsis,
                                   style: theme.textTheme.bodySmall?.copyWith(
                                     color: Colors.orange.shade700,
                                   ),
                                 ),
                             ],
                           ),
                         ),
                         TextButton(
                           onPressed: () {
                             ref.read(importNotifierProvider.notifier)
                                 .retryParseCandidate(item.index);
                             setState(() {}); // 刷新 UI
                           },
                           child: const Text('重试'),
                         ),
                         TextButton(
                           onPressed: () {
                             // 手动编辑：导航回预览页
                             context.go('/import/preview/${state.jobId}');
                           },
                           child: const Text('手动编辑'),
                         ),
                       ],
                     ),
                   );
                 }),
               ],
             ),
           ),
         ),
       ]
       ```

    2. **调整 CTA 按钮区域 padding**：CTA 按钮的 `const SizedBox(height: 32)` 移到跳过区域之前（避免间隔过大）。

    3. **在 GoRouter 中添加 redirect 守卫**（router.dart）：
       在文件顶部添加 import：
       ```dart
       import 'package:flutter_riverpod/flutter_riverpod.dart';
       import '../features/import/providers/import_notifier.dart';
       ```

       修改 `/import/preview/:jobId` 路由：
       ```dart
       GoRoute(
         path: '/import/preview/:jobId',
         redirect: (BuildContext context, GoRouterState state) {
           final container = ProviderScope.containerOf(context);
           final importState = container.read(importNotifierProvider);
           if (!importState.isEditing && !importState.isCommitting) {
             return '/';
           }
           return null;
         },
         builder: (BuildContext context, GoRouterState state) =>
             const ImportPreviewScreen(),
       ),
       ```

       修改 `/import/summary/:jobId` 路由：
       ```dart
       GoRoute(
         path: '/import/summary/:jobId',
         redirect: (BuildContext context, GoRouterState state) {
           final container = ProviderScope.containerOf(context);
           final importState = container.read(importNotifierProvider);
           if (!importState.isDone && importState.committedCount == 0) {
             return '/';
           }
           return null;
         },
         builder: (BuildContext context, GoRouterState state) =>
             const ImportSummaryScreen(),
       ),
       ```

    4. ImportSummaryScreen.initState 中的守卫（第27-36行）可以保留作为防御层，与 router redirect 互补。
  </action>

  <verify>
    <automated>grep -n "skippedCandidates" lib/features/import/presentation/import_summary_screen.dart && grep -n "retryParseCandidate" lib/features/import/presentation/import_summary_screen.dart && grep -n "redirect:" lib/routing/router.dart</automated>
  </verify>

  <acceptance_criteria>
    - `ImportSummaryScreen` 在成功统计卡片下方展示跳过题目区域（仅当 `skippedCandidates.isNotEmpty` 时渲染）
    - 每条跳过项显示：'#{序号}: {原因}' + 题干预览（1行截断）+ '重试' TextButton + '手动编辑' TextButton
    - '重试' 按钮调用 `ImportNotifier.retryParseCandidate(item.index)`
    - '手动编辑' 按钮导航到 `context.go('/import/preview/${state.jobId}')`
    - 跳过区域使用橙色警告色调（`Colors.orange.shade50` 背景卡片 + `warning_amber_rounded` 图标）
    - 当无跳过项时，整个跳过区域不可见
    - `/import/preview/:jobId` 路由有 `redirect` 回调——ImportNotifier 不在 editing/committing 状态时重定向到 `/`
    - `/import/summary/:jobId` 路由有 `redirect` 回调——ImportNotifier 不在 done 状态且 committedCount==0 时重定向到 `/`
    - `flutter analyze lib/routing/router.dart lib/features/import/presentation/import_summary_screen.dart` 无错误
  </acceptance_criteria>

  <done>摘要页展示跳过题目列表含重试/手动编辑按钮；过期深链接自动重定向到首页。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GoRouter redirect → ProviderScope.containerOf | 路由守卫读取 Riverpod 状态——ProviderScope 必须在 GoRouter 初始化之前存在于 widget 树中 |
| 用户输入 bankName → ImportState | 自由文本输入通过 setBankName() 写入 state，无持久化风险（名称最终写入 DB 时 drift 做参数化） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Spoofing | GoRouter redirect bypass via direct navigation | accept | GoRouter redirect 是客户端级守卫——用户可绕过（例如通过浏览器刷新），但 ImportPreviewScreen.initState 作为第二层防御。低风险：过期 jobId 访问仅导致空状态显示。 |
| T-02-03 | Information Disclosure | skippedCandidates reason strings | accept | 跳过原因字符串为用户界面友好消息（'答案未识别'、'用户跳过'）——不含敏感数据或系统内部信息。 |
</threat_model>

<verification>
- [ ] 预览页顶部题库名称 TextField 预填充文件名，编辑后实时验证 CJK 字符计数
- [ ] 输入空名称点击保存 → 显示 '请输入题库名称' 错误，提交被阻止
- [ ] 输入超长名称（中英文混合超过20等效字符） → 显示 '题库名称过长' 错误
- [ ] 摘要页展示橙色跳过区域，含跳过原因和重试/手动编辑按钮
- [ ] 点击"重试" → 该题目重新解析并加入确认集，跳过列表即时更新
- [ ] 点击"手动编辑" → 导航回预览页
- [ ] 直接访问 /import/preview/nonexistent → 重定向到 /
- [ ] 直接访问 /import/summary/nonexistent → 重定向到 /
- [ ] `flutter analyze` 全部修改文件零错误
</verification>

<success_criteria>
1. D-18 完全实现：题库名称可编辑，CJK 感知 20 字符限制实时验证，空名阻止提交
2. D-09 完全实现：摘要页展示跳过题目列表，含逐条重试和手动编辑入口
3. 路由守卫到位：过期 jobId 深链接自动重定向到首页
4. 全链路完整：导入 → 编辑名称 → 审核 → 提交 → 摘要（含跳过项+重试） → 复习
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-file-import-pipeline/02-03-SUMMARY.md`
</output>
