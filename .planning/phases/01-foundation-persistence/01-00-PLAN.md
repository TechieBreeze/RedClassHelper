---
phase: 01-foundation-persistence
plan: 00
type: tool
wave: 0
depends_on: []
files_modified:
  - C:/Users/Lenovo/fvm/versions/3.35.7/bin/flutter.bat
  - C:/Users/Lenovo/AppData/Local/Android/Sdk/platform-tools/adb.exe
  - "C:/Program Files/Microsoft Visual Studio/2022/Community/MSBuild/Current/Bin/MSBuild.exe"
autonomous: false
user_decisions: []
requirements: []

must_haves:
  truths:
    - "`flutter --version` returns 3.35.7+ on PATH"
    - "`flutter doctor` shows green for Windows / Linux / Android sections"
    - "Visual Studio 2022 with 'Desktop development with C++' workload installed"
    - "Android SDK + platform-tools installed (adb on PATH)"
    - "build_runner is reachable via `dart run build_runner --help`"
user_setup: []
---

<objective>
Install the Flutter 3.35.7 toolchain (Flutter SDK + Android SDK + Visual Studio 2022 C++ workload) on the Windows dev host and verify `flutter doctor` is green for the 3 v1 platform targets (Windows / Linux / Android). Phase 1 cannot execute any other plan until this completes — every downstream `flutter create` / `flutter pub get` / `flutter build` depends on it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-persistence/01-RESEARCH.md
</context>

<verification>
- [ ] `flutter --version` exits 0 and prints `Flutter 3.35.7` (or higher 3.35.x stable)
- [ ] `flutter doctor` exits 0 with `✓` for: `Flutter`, `Windows` toolchain, `Android` toolchain, and `Connected device` (or at minimum, no `✗` for required tools)
- [ ] `dart --version` exits 0 and prints Dart 3.9.2 (or compatible)
- [ ] `adb --version` exits 0 (Android platform-tools on PATH)
- [ ] `where flutter` returns the expected Flutter SDK path
</verification>

<tasks>

<task name="task-install-flutter-sdk">
<read_first>
- file:.planning/PROJECT.md — Constraint: "Tech stack: Flutter (Dart) — must support Windows / Linux / Android"
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — STACK.md pinned Flutter 3.35.7 stable
</read_first>
<action>
1. Download Flutter SDK 3.35.7 stable: `https://docs.flutter.dev/get-started/install/windows` (or use `git clone https://github.com/flutter/flutter.git -b stable --depth 1` then `flutter doctor` to trigger the channel upgrade to 3.35.7).
2. Extract to `C:\src\flutter` (avoid `C:\Program Files\` — needs write access for `pub-cache` and updates) OR `C:\Users\Lenovo\flutter`.
3. Add `C:\src\flutter\bin` (or chosen path) to user PATH. Close and reopen Git Bash to pick up the new PATH.
4. Verify: `flutter --version` prints 3.35.7 and `dart --version` prints Dart 3.9.2+.
5. If Git Bash throws a "line 1: $'\r': command not found" error, run `git config --global core.autocrlf false` (Flutter SDK on Windows has CRLF line endings; this is the documented workaround).
6. Run `flutter doctor --android-licenses` and accept all licenses (this will fail if Android SDK is not yet installed — re-run after Android SDK step).
7. (Optional) Install FVM: `dart pub global activate fvm` and pin the project to `fvm use 3.35.7` — not required, but useful if you plan to maintain multiple Flutter versions.
</action>
<acceptance_criteria>
- `flutter --version` exits 0 with output containing "Flutter 3.35.7" and "Dart 3.9.2"
- `where flutter` (cmd) or `which flutter` (bash) returns a path under `C:\src\flutter\bin\` or chosen SDK location
- `dart --version` exits 0 with output containing "Dart 3.9.2" or compatible 3.9.x
- After step 6, if Android SDK is not yet installed, proceed to next task; otherwise all licenses are accepted
</acceptance_criteria>
</task>

<task name="task-install-android-sdk">
<read_first>
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — STACK.md line "Android SDK: User must install via Android Studio or `sdkmanager`"
- file:.planning/phases/01-foundation-persistence/01-VALIDATION.md — `flutter build apk --debug` is the success criterion for Android toolchain
</read_first>
<action>
1. Install Android Studio (Hedgehog 2023.1.1 or later) — `https://developer.android.com/studio` — or use the command-line SDK tools: `https://developer.android.com/studio#command-line-tools-only`.
2. Launch Android Studio → SDK Manager → install:
   - Android SDK Platform 33 (or 34, matches `compileSdkVersion` Flutter 3.35.7 expects)
   - Android SDK Build-Tools 34.0.0
   - Android SDK Platform-Tools
   - Android SDK Command-line Tools (latest)
3. Set `ANDROID_HOME=C:\Users\Lenovo\AppData\Local\Android\Sdk` (default Android Studio install path) and add `%ANDROID_HOME%\platform-tools` to user PATH.
4. Verify in Git Bash: `adb --version` exits 0; `sdkmanager --list_installed` shows installed platforms.
5. Accept all licenses: `flutter doctor --android-licenses` (type `y` for each).
6. Re-run `flutter doctor` and confirm `Android toolchain - develop for Android devices` shows a green checkmark.
</action>
<acceptance_criteria>
- `adb --version` exits 0 and prints "Android Debug Bridge version 1.0.41" or compatible
- `sdkmanager --list_installed` exits 0 and lists at least one `platforms;android-XX` (e.g., `platforms;android-33`)
- `flutter doctor` Android toolchain section shows `✓` (green checkmark) — no `✗ Android Studio` or `✗ cmdline-tools`
- `flutter doctor` no longer reports any license-related errors
</acceptance_criteria>
</task>

<task name="task-install-visual-studio-2022">
<read_first>
- file:.planning/PROJECT.md — Platform constraint: "Windows + Linux + Android" packaging required
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — STACK.md line "Visual Studio 2022 (Windows desktop workload): User must install VS2022 with 'Desktop development with C++' + 'Flutter' components"
</read_first>
<action>
1. Download Visual Studio 2022 Community edition (free): `https://visualstudio.microsoft.com/vs/community/`.
2. In the VS Installer, select the "Desktop development with C++" workload. Also include:
   - "C++ build tools core features"
   - "C++ CMake tools for Windows"
   - "C++ MFC for latest build tools (x86 & x64)"
   - "Windows 11 SDK" (or Windows 10 SDK if you prefer)
3. Click Install (or Modify if VS is already present). This is a 6-10 GB download — schedule accordingly.
4. After install, open "Developer Command Prompt for VS 2022" and run `where cl.exe` to confirm MSVC compiler is on PATH in that environment. (Git Bash users do NOT need VS on PATH globally — `flutter build windows` finds it via registry.)
5. Re-run `flutter doctor` and confirm `Visual Studio - develop Windows apps` shows `✓` (green).
6. Verify: `flutter config --enable-windows-desktop` returns the current setting (default is already enabled on Windows hosts).
</action>
<acceptance_criteria>
- `flutter doctor` "Visual Studio - develop Windows apps" section shows `✓` (green)
- `where cl.exe` in "Developer Command Prompt for VS 2022" returns `C:\Program Files\Microsoft Visual Studio\2022\...\bin\Hostx64\x64\cl.exe`
- `flutter config --enable-windows-desktop` returns "Setting `enable-windows-desktop` to `true`" (or "already true")
- `flutter config --enable-linux-desktop` returns the same (Linux desktop is automatically available when Flutter is on Windows; this confirms Flutter is aware of the Linux target)
</acceptance_criteria>
</task>

<task name="task-verify-build-toolchain">
<read_first>
- file:.planning/phases/01-foundation-persistence/01-VALIDATION.md — `flutter build <platform> --debug` per platform is the smoke test command
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — A6 assumption: "The user's Windows dev machine will have Flutter installed before Phase 1 plans execute"
</read_first>
<action>
1. Open a fresh Git Bash terminal (or close and reopen existing one) to pick up all PATH changes.
2. Run `flutter doctor -v` and save the full output to `.planning/phases/01-foundation-persistence/.toolchain-baseline.txt` for the Phase 1 audit trail.
3. Verify all 3 v1 platforms (Windows, Linux, Android) have green toolchain sections:
   - `Flutter` ✓
   - `Windows` toolchain ✓ (or `Visual Studio - develop Windows apps` ✓)
   - `Android toolchain` ✓
   - `Linux` toolchain — may show as "Not applicable" on Windows host (this is OK; Linux is a target Flutter cross-compiles to, requires WSL or a Linux host for actual build — see note in 01-RESEARCH.md)
4. Run `flutter create --template=app --platforms=windows /tmp/flutter_smoke` to do a no-op smoke test of the toolchain; if it succeeds without errors, delete the smoke project: `rm -rf /tmp/flutter_smoke`.
5. If any required section shows `✗` or `!`, troubleshoot per `flutter doctor`'s diagnostic output and re-run until green.
6. Commit `.toolchain-baseline.txt` if you want a record (or skip — this is a build-environment artifact, not source code).
</action>
<acceptance_criteria>
- `flutter doctor -v` exits 0 and all required toolchain sections (Flutter, Windows, Android) show `✓` green
- The file `.planning/phases/01-foundation-persistence/.toolchain-baseline.txt` exists and contains the full `flutter doctor -v` output (≥ 30 lines)
- `flutter create --template=app --platforms=windows /tmp/flutter_smoke` exits 0 (proves the full toolchain can scaffold a project)
- After smoke test, `/tmp/flutter_smoke` is removed (clean state)
- `flutter doctor` shows 0 critical issues for the 3 v1 platforms
</acceptance_criteria>
</task>

</tasks>

<success_criteria>
- Flutter 3.35.7 SDK installed at a known path and on user PATH
- `flutter --version` returns 3.35.7, `dart --version` returns 3.9.2+
- Android SDK with platform-33+ installed; `adb --version` exits 0
- Visual Studio 2022 with "Desktop development with C++" workload installed
- `flutter doctor` shows green for: Flutter, Windows, Android toolchains
- `.toolchain-baseline.txt` audit trail saved for the Phase 1 record
- `flutter create` smoke test passes — proves the toolchain can actually scaffold a project
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-persistence/01-00-SUMMARY.md`
</output>
