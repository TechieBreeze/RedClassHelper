---
phase: 01-foundation-persistence
plan: 04
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/routing/router.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/features/home/presentation/home_screen.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/features/bank_detail/presentation/bank_detail_screen.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/features/quiz/presentation/quiz_screen.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/features/stats/presentation/stats_screen.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/features/bookmarks/presentation/bookmarks_screen.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/features/import/presentation/import_screen.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/lib/main.dart
  - C:/Users/Lenovo/Desktop/agent-workspace/narrowfork/workspace/RedClass/test/routing/router_test.dart
autonomous: true
user_decisions: []
requirements:
  - PLT-04

must_haves:
  truths:
    - "go_router config defines exactly 6 routes: `/`, `/bank/:id`, `/quiz/:bankId/:mode`, `/stats`, `/bookmarks`, `/import`"
    - "Each route renders a `Scaffold + AppBar(title) + Center(Text('TODO'))` placeholder (or the home screen for `/`)"
    - "`MaterialApp.router(routerConfig: appRouter)` replaces the placeholder `MaterialApp(home: ...)` in `lib/main.dart`"
    - "`errorBuilder` returns a Scaffold with `Route not found: ${state.uri}` message"
    - "Home screen placeholder renders without errors (bank empty state — full layout in plan 01-05)"
    - "Static grep guard: `grep -r \"Navigator.push\\|Navigator.of(context).push\" lib/ --include=\"*.dart\"` returns 0 lines (go_router is the only nav API)"
    - "Unit test asserts that `appRouter` is constructible and has 6 routes (via reflection or config inspection)"
    - "`flutter analyze` exits 0"
user_setup: []
---

<objective>
Introduce `go_router` as the sole navigation API in the app (per ROADMAP Phase 1 success criterion #6 and UI-SPEC "Routes" table). Define the 6 routes (`/`, `/bank/:id`, `/quiz/:bankId/:mode`, `/stats`, `/bookmarks`, `/import`) in `lib/routing/router.dart`. Create placeholder screens for each route under `lib/features/<feature>/presentation/` — each is a `Scaffold + AppBar(title) + Center(Text('TODO'))`. The `/` route renders a basic home screen (full home layout per UI-SPEC is in plan 01-05; this plan just creates the home screen with the correct route structure so plan 01-05 can layer on the bank empty state, mode tiles, stats entry). Switch `lib/main.dart` to use `MaterialApp.router(routerConfig: appRouter)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-persistence/01-CONTEXT.md
@.planning/phases/01-foundation-persistence/01-UI-SPEC.md
@.planning/phases/01-foundation-persistence/01-RESEARCH.md
</context>

<interfaces>
From `01-UI-SPEC.md` "Routes" section (LOCKED route paths):
- `/` → HomeScreen (implemented in plan 01-05; here it's a minimal Scaffold)
- `/bank/:id` → BankDetailScreen placeholder
- `/quiz/:bankId/:mode` → QuizScreen placeholder (note: Phase 1 doesn't validate mode values)
- `/stats` → StatsScreen placeholder
- `/bookmarks` → BookmarksScreen placeholder
- `/import` → ImportScreen placeholder (Phase 2 will platform-branch this; Phase 1 just shows TODO)

From `01-UI-SPEC.md` "Component Inventory §Placeholder screen":
> "Scaffold + AppBar + Center(Text('TODO'))"
> "For /bank/:id, /quiz/:bankId/:mode, /stats, /bookmarks, /import"

From `01-RESEARCH.md` "Example 7: go_router Config with 6 Routes" + "Pitfall 5: go_router Must Be the Only Navigation API":
- 6 `GoRoute(path: ..., builder: ...)` declarations
- `appRouter` exported as a `final` `GoRouter` instance
- `MaterialApp.router(routerConfig: appRouter)` in main
- `errorBuilder` for unknown routes
- `grep -r "Navigator.push" lib/` must return 0 lines
</interfaces>

<tasks>

<task name="task-create-placeholder-screens">
<read_first>
- file:.planning/phases/01-foundation-persistence/01-UI-SPEC.md — "Routes" table — exact AppBar titles for each placeholder
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — "Pitfall 5: go_router Must Be the Only Navigation API"
- file:.planning/phases/01-foundation-persistence/01-CONTEXT.md — D-02 (folder layout)
</read_first>
<action>
1. **Create 6 placeholder screen files** (one per route, under `lib/features/<feature>/presentation/`):

   **a. `lib/features/home/presentation/home_screen.dart`** (Phase 1 minimal — plan 01-05 adds the full layout):
   ```dart
   // lib/features/home/presentation/home_screen.dart
   import 'package:flutter/material.dart';
   import 'package:go_router/go_router.dart';

   /// 主页 — Phase 1 占位,计划 01-05 添加题库空态 + 三模式入口 + 数据统计入口
   class HomeScreen extends StatelessWidget {
     const HomeScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(
           title: const Text('红课复习'),
         ),
         body: Center(
           child: Column(
             mainAxisAlignment: MainAxisAlignment.center,
             children: [
               const Text('Home — 占位,完整布局在 01-05'),
               const SizedBox(height: 16),
               FilledButton.tonal(
                 onPressed: () => context.go('/stats'),
                 child: const Text('前往 /stats (验证路由)'),
               ),
             ],
           ),
         ),
       );
     }
   }
   ```

   **b. `lib/features/bank_detail/presentation/bank_detail_screen.dart`**:
   ```dart
   import 'package:flutter/material.dart';

   class BankDetailScreen extends StatelessWidget {
     const BankDetailScreen({super.key, required this.bankId});

     final String bankId;

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('题库详情')),
         body: Center(
           child: Text('TODO — BankDetailScreen for $bankId\n(完整实现见 Phase 4)'),
         ),
       );
     }
   }
   ```

   **c. `lib/features/quiz/presentation/quiz_screen.dart`**:
   ```dart
   import 'package:flutter/material.dart';

   class QuizScreen extends StatelessWidget {
     const QuizScreen({super.key, required this.bankId, required this.mode});

     final String bankId;
     final String mode;

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('答题')),
         body: Center(
           child: Text('TODO — QuizScreen bankId=$bankId mode=$mode\n(完整实现见 Phase 4)'),
         ),
       );
     }
   }
   ```

   **d. `lib/features/stats/presentation/stats_screen.dart`**:
   ```dart
   import 'package:flutter/material.dart';

   class StatsScreen extends StatelessWidget {
     const StatsScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('数据统计')),
         body: const Center(
           child: Text('TODO — StatsScreen\n(完整实现见 Phase 5)'),
         ),
       );
     }
   }
   ```

   **e. `lib/features/bookmarks/presentation/bookmarks_screen.dart`**:
   ```dart
   import 'package:flutter/material.dart';

   class BookmarksScreen extends StatelessWidget {
     const BookmarksScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('收藏夹')),
         body: const Center(
           child: Text('TODO — BookmarksScreen\n(完整实现见 Phase 5)'),
         ),
       );
     }
   }
   ```

   **f. `lib/features/import/presentation/import_screen.dart`**:
   ```dart
   import 'package:flutter/material.dart';

   class ImportScreen extends StatelessWidget {
     const ImportScreen({super.key});

     @override
     Widget build(BuildContext context) {
       return Scaffold(
         appBar: AppBar(title: const Text('导入题库')),
         body: const Center(
           child: Text('TODO — ImportScreen\n(Phase 2 实现桌面端 .docx/.pdf/.json;Phase 5 加 Android .json 入口)'),
         ),
       );
     }
   }
   ```

2. **Verify all 6 screen files exist**:
   ```bash
   find lib/features -name "*_screen.dart" | sort
   ```
   Expected: 6 files matching the list above.

3. **Run `flutter analyze`** — must exit 0 (the screens are self-contained, no missing imports).
</action>
<acceptance_criteria>
- All 6 screen files exist under `lib/features/<feature>/presentation/`
- `find lib/features -name "*_screen.dart" | wc -l` returns 6
- Each screen is a `StatelessWidget` with `Scaffold(appBar: AppBar(title: ...), body: Center(...))`
- `grep -q "题库详情" lib/features/bank_detail/presentation/bank_detail_screen.dart` returns 0
- `grep -q "答题" lib/features/quiz/presentation/quiz_screen.dart` returns 0
- `grep -q "数据统计" lib/features/stats/presentation/stats_screen.dart` returns 0
- `grep -q "收藏夹" lib/features/bookmarks/presentation/bookmarks_screen.dart` returns 0
- `grep -q "导入题库" lib/features/import/presentation/import_screen.dart` returns 0
- `grep -q "红课复习" lib/features/home/presentation/home_screen.dart` returns 0
- `flutter analyze` exits 0
</acceptance_criteria>
</task>

<task name="task-create-go-router-config">
<read_first>
- file:.planning/phases/01-foundation-persistence/01-UI-SPEC.md — "Routes" table
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — "Example 7: go_router Config with 6 Routes" + "Pitfall 5"
</read_first>
<action>
1. **Create `lib/routing/router.dart`** with the 6 GoRoute declarations:
   ```dart
   // lib/routing/router.dart
   import 'package:flutter/material.dart';
   import 'package:go_router/go_router.dart';

   import '../features/bank_detail/presentation/bank_detail_screen.dart';
   import '../features/bookmarks/presentation/bookmarks_screen.dart';
   import '../features/home/presentation/home_screen.dart';
   import '../features/import/presentation/import_screen.dart';
   import '../features/quiz/presentation/quiz_screen.dart';
   import '../features/stats/presentation/stats_screen.dart';

   /// 全局 GoRouter 实例 — 唯一允许的导航 API (UI-SPEC §Routes)
   final GoRouter appRouter = GoRouter(
     initialLocation: '/',
     routes: <RouteBase>[
       GoRoute(
         path: '/',
         builder: (BuildContext context, GoRouterState state) =>
             const HomeScreen(),
       ),
       GoRoute(
         path: '/bank/:id',
         builder: (BuildContext context, GoRouterState state) =>
             BankDetailScreen(bankId: state.pathParameters['id']!),
       ),
       GoRoute(
         path: '/quiz/:bankId/:mode',
         builder: (BuildContext context, GoRouterState state) => QuizScreen(
           bankId: state.pathParameters['bankId']!,
           mode: state.pathParameters['mode']!,
         ),
       ),
       GoRoute(
         path: '/stats',
         builder: (BuildContext context, GoRouterState state) =>
             const StatsScreen(),
       ),
       GoRoute(
         path: '/bookmarks',
         builder: (BuildContext context, GoRouterState state) =>
             const BookmarksScreen(),
       ),
       GoRoute(
         path: '/import',
         builder: (BuildContext context, GoRouterState state) =>
             const ImportScreen(),
       ),
     ],
     errorBuilder: (BuildContext context, GoRouterState state) => Scaffold(
       appBar: AppBar(title: const Text('路由错误')),
       body: Center(
         child: Text('Route not found: ${state.uri}'),
       ),
     ),
   );
   ```

2. **Verify**:
   ```bash
   ls -la lib/routing/
   grep -c "GoRoute" lib/routing/router.dart
   ```
   Expected: `router.dart` exists; `grep -c "GoRoute"` returns 6 (one per route).

3. **Run `flutter analyze`** — must exit 0.
</action>
<acceptance_criteria>
- File `lib/routing/router.dart` exists
- `grep -q "final GoRouter appRouter" lib/routing/router.dart` returns 0
- `grep -c "GoRoute(" lib/routing/router.dart` returns 6
- `grep -q "path: '/'" lib/routing/router.dart` returns 0
- `grep -q "path: '/bank/:id'" lib/routing/router.dart` returns 0
- `grep -q "path: '/quiz/:bankId/:mode'" lib/routing/router.dart` returns 0
- `grep -q "path: '/stats'" lib/routing/router.dart` returns 0
- `grep -q "path: '/bookmarks'" lib/routing/router.dart` returns 0
- `grep -q "path: '/import'" lib/routing/router.dart` returns 0
- `grep -q "errorBuilder" lib/routing/router.dart` returns 0
- `grep -q "initialLocation: '/'" lib/routing/router.dart` returns 0
- `flutter analyze` exits 0
</acceptance_criteria>
</task>

<task name="task-wire-mai-napp-to-router">
<read_first>
- file:.planning/phases/01-foundation-persistence/01-RESEARCH.md — "Example 8: MaterialApp.router Setup with themeMode: system"
- file:.planning/phases/01-foundation-persistence/01-UI-SPEC.md — D-21 (ThemeMode.system)
</read_first>
<action>
1. **Update `lib/main.dart`** — replace `MaterialApp(home: ...)` with `MaterialApp.router(routerConfig: appRouter)`:
   ```dart
   // lib/main.dart
   import 'package:flutter/material.dart';
   import 'package:flutter_riverpod/flutter_riverpod.dart';
   import 'package:redclass/core/paths.dart';
   import 'package:redclass/routing/router.dart';

   Future<void> main() async {
     WidgetsFlutterBinding.ensureInitialized();
     // RESEARCH.md Pitfall 4: 预解析 path_provider 路径,避免 ref.watch 时的 late-init 错误
     final resolver = await PathResolver.create();
     runApp(
       ProviderScope(
         overrides: [
           pathResolverProvider.overrideWith((ref) async => resolver),
         ],
         child: const RedClassApp(),
       ),
     );
   }

   class RedClassApp extends StatelessWidget {
     const RedClassApp({super.key});

     @override
     Widget build(BuildContext context) {
       return MaterialApp.router(
         title: '红课复习',
         theme: ThemeData(
           useMaterial3: true,
           colorScheme: ColorScheme.fromSeed(
             seedColor: const Color(0xFF6750A4),
             brightness: Brightness.light,
           ),
         ),
         darkTheme: ThemeData(
           useMaterial3: true,
           colorScheme: ColorScheme.fromSeed(
             seedColor: const Color(0xFF6750A4),
             brightness: Brightness.dark,
           ),
         ),
         themeMode: ThemeMode.system, // D-21
         routerConfig: appRouter, // Phase 1 routing
         locale: const Locale('zh', 'CN'),
       );
     }
   }
   ```

2. **Static grep guard for Navigator.push**:
   ```bash
   grep -rn "Navigator.push\|Navigator.of(context).push" lib/ --include="*.dart"
   ```
   Expected: 0 lines (go_router is the only navigation API per UI-SPEC).

3. **Run `flutter analyze`** — must exit 0.

4. **Run `flutter test`** — all tests pass (path_resolver_test, migration_test).
</action>
<acceptance_criteria>
- `lib/main.dart` uses `MaterialApp.router(routerConfig: appRouter, ...)` instead of `MaterialApp(home: ...)`
- `grep -q "MaterialApp.router" lib/main.dart` returns 0
- `grep -q "routerConfig: appRouter" lib/main.dart` returns 0
- `grep -q "locale: const Locale('zh', 'CN')" lib/main.dart` returns 0 (Chinese locale per UI-SPEC)
- `grep -q "ThemeMode.system" lib/main.dart` returns 0
- `grep -rn "Navigator.push\|Navigator.of(context).push" lib/ --include="*.dart" | wc -l` returns 0
- `flutter analyze` exits 0
- `flutter test` exits 0
</acceptance_criteria>
</task>

<task name="task-write-router-smoke-test">
<read_first>
- file:.planning/phases/01-foundation-persistence/01-VALIDATION.md — "Wave 0 Gaps" — `test/routing/router_test.dart`
- file:.planning/phases/01-foundation-persistence/01-UI-SPEC.md — 6 routes with their path patterns
</read_first>
<action>
1. **Create `test/routing/router_test.dart`**:
   ```dart
   // test/routing/router_test.dart
   import 'package:flutter/material.dart';
   import 'package:flutter_test/flutter_test.dart';
   import 'package:redclass/routing/router.dart';

   void main() {
     group('appRouter (go_router — 6 routes)', () {
       test('initial location is /', () {
         expect(appRouter.routerDelegate.currentConfiguration.uri.path, '/');
       });

       test('routes are registered for all 6 paths', () {
         // Inspect GoRouter's internal configuration via the configuration list
         final configuration = appRouter.configuration;
         final paths = configuration.routes
             .whereType<GoRoute>()
             .map((r) => r.path)
             .toList();
         expect(paths, containsAll(<String>[
           '/',
           '/bank/:id',
           '/quiz/:bankId/:mode',
           '/stats',
           '/bookmarks',
           '/import',
         ]));
         expect(paths.length, 6);
       });

       testWidgets('navigates to /stats renders StatsScreen', (tester) async {
         await tester.pumpWidget(MaterialApp.router(routerConfig: appRouter));
         await tester.pumpAndSettle();
         // Initial = home; navigate to /stats
         appRouter.go('/stats');
         await tester.pumpAndSettle();
         expect(find.text('数据统计'), findsOneWidget);
       });

       testWidgets('navigates to /import renders ImportScreen', (tester) async {
         await tester.pumpWidget(MaterialApp.router(routerConfig: appRouter));
         await tester.pumpAndSettle();
         appRouter.go('/import');
         await tester.pumpAndSettle();
         expect(find.text('导入题库'), findsOneWidget);
       });

       testWidgets('navigates to /bookmarks renders BookmarksScreen', (tester) async {
         await tester.pumpWidget(MaterialApp.router(routerConfig: appRouter));
         await tester.pumpAndSettle();
         appRouter.go('/bookmarks');
         await tester.pumpAndSettle();
         expect(find.text('收藏夹'), findsOneWidget);
       });

       testWidgets('navigates to /bank/some-id renders BankDetailScreen', (tester) async {
         await tester.pumpWidget(MaterialApp.router(routerConfig: appRouter));
         await tester.pumpAndSettle();
         appRouter.go('/bank/some-id');
         await tester.pumpAndSettle();
         expect(find.text('题库详情'), findsOneWidget);
         expect(find.textContaining('some-id'), findsOneWidget);
       });

       testWidgets('navigates to /quiz/bank-1/random renders QuizScreen', (tester) async {
         await tester.pumpWidget(MaterialApp.router(routerConfig: appRouter));
         await tester.pumpAndSettle();
         appRouter.go('/quiz/bank-1/random');
         await tester.pumpAndSettle();
         expect(find.text('答题'), findsOneWidget);
         expect(find.textContaining('bank-1'), findsOneWidget);
         expect(find.textContaining('random'), findsOneWidget);
       });

       testWidgets('unknown route renders errorBuilder', (tester) async {
         await tester.pumpWidget(MaterialApp.router(routerConfig: appRouter));
         await tester.pumpAndSettle();
         appRouter.go('/this-route-does-not-exist');
         await tester.pumpAndSettle();
         expect(find.textContaining('Route not found'), findsOneWidget);
       });
     });
   }
   ```

2. **Run the test**:
   ```bash
   flutter test test/routing/router_test.dart
   ```
   Expected: all 8 tests pass.

3. **Run full test suite**:
   ```bash
   flutter test
   ```
   Expected: all tests pass (path_resolver_test, migration_test, router_test).
</action>
<acceptance_criteria>
- File `test/routing/router_test.dart` exists
- `flutter test test/routing/router_test.dart` exits 0 with all 8 tests passing
- `flutter test` exits 0 (full suite, 3 test files)
- `grep -c "test(" test/routing/router_test.dart` returns 8 (or more)
- `grep -q "configuration.routes" test/routing/router_test.dart` returns 0 (route inspection)
- `grep -q "errorBuilder" test/routing/router_test.dart` returns 0 (or `Route not found` test)
- All 6 routes are tested: /, /bank/:id, /quiz/:bankId/:mode, /stats, /bookmarks, /import, plus the error path
</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route path ↔ user input | `pathParameters` come from the URL; should be treated as untrusted input if used for DB queries (Phase 2+ concern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T / EoP | User-controlled URL → path traversal in `bankId`/`mode` | mitigate (Phase 4+) | In Phase 1, `bankId`/`mode` are only rendered as `Text(...)`. Phase 4 quiz logic MUST validate `bankId` exists in DB before fetching questions. Documented in plan 01-04 SUMMARY |
| T-04-02 | Repudiation | `Navigator.push` smuggled in alongside `go_router` | mitigate | Static grep guard: `grep -rn "Navigator.push" lib/` must return 0 lines; included in plan 01-04's acceptance criteria |
| T-04-03 | Information Disclosure | Unknown route reveals internal state via `state.uri` | accept | `errorBuilder` only shows the URI the user typed; no internal state leaked. v1 acceptable |
| T-04-04 | DoS | Deep link to invalid route causes infinite loader | mitigate | `errorBuilder` always returns a Scaffold (no async work); tested in `test/routing/router_test.dart` |
</threat_model>

<verification>
- [ ] 6 placeholder screens exist under `lib/features/<feature>/presentation/`
- [ ] `lib/routing/router.dart` defines 6 `GoRoute`s with paths: `/`, `/bank/:id`, `/quiz/:bankId/:mode`, `/stats`, `/bookmarks`, `/import`
- [ ] `lib/main.dart` uses `MaterialApp.router(routerConfig: appRouter)` with `ThemeMode.system` and `Locale('zh', 'CN')`
- [ ] `flutter analyze` exits 0
- [ ] `flutter test test/routing/router_test.dart` passes 8 tests
- [ ] `flutter test` (full suite) passes
- [ ] Static grep: no `Navigator.push` anywhere in `lib/`
</verification>

<success_criteria>
- 6 go_router routes registered and navigable
- 6 placeholder screens with correct AppBar titles (题库详情 / 答题 / 数据统计 / 收藏夹 / 导入题库 / 红课复习)
- HomeScreen minimal placeholder ready for plan 01-05 to layer on full layout
- `MaterialApp.router` wired in main with `ThemeMode.system` and Chinese locale
- go_router is the only navigation API (no `Navigator.push` anywhere)
- All tests pass; flutter analyze clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-persistence/01-04-SUMMARY.md`
</output>
