---
phase: 02-desktop-file-import-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/import/providers/import_notifier.g.dart
  - lib/features/import/parsing/parse_candidate.g.dart
autonomous: true
gap_closure: true
requirements:
  - IMP-01
  - IMP-02
  - IMP-04
  - QST-03
  - UI-04

must_haves:
  truths:
    - "编译可用——`flutter analyze` 零错误通过"
    - "import_notifier.g.dart 生成并存在于文件系统"
  artifacts:
    - path: "lib/features/import/providers/import_notifier.g.dart"
      provides: "Riverpod @riverpod 代码生成输出"
      contains: "class ImportNotifier extends _$ImportNotifier"
    - path: "lib/features/import/parsing/parse_candidate.g.dart"
      provides: "json_serializable 代码生成输出"
      contains: "_$ParseCandidateFromJson"
  key_links: []
---

<objective>
运行 Dart build_runner 生成所有缺失的 `.g.dart` 文件，解除编译阻断。

Purpose: VERIFICATION.md Gap #1 — `import_notifier.g.dart` 缺失导致项目完全无法编译。所有其他修复都依赖于此。
Output: 生成 `import_notifier.g.dart` 和 `parse_candidate.g.dart` 文件，`flutter analyze` 零错误通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-desktop-file-import-pipeline/02-VERIFICATION.md
@.planning/phases/02-desktop-file-import-pipeline/02-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 运行 build_runner 生成缺失的代码文件</name>
  <files>lib/features/import/providers/import_notifier.g.dart, lib/features/import/parsing/parse_candidate.g.dart</files>

  <read_first>
    - .planning/phases/02-desktop-file-import-pipeline/02-VERIFICATION.md §差距摘要 #1 — 阻断：import_notifier.g.dart 缺失
    - lib/features/import/providers/import_notifier.dart — 第20行 part 'import_notifier.g.dart' 引用了缺失文件
    - lib/features/import/parsing/parse_candidate.dart — 第7行 part 'parse_candidate.g.dart' 引用了缺失文件
    - pubspec.yaml — 确认 build_runner / riverpod_generator / json_serializable 已在 dev_dependencies 中
  </read_first>

  <action>
    1. 运行 build_runner 生成所有 `.g.dart` 文件：
       ```
       cd C:\Users\Lenovo\Desktop\agent-workspace\narrowfork\workspace\RedClass
       dart run build_runner build --delete-conflicting-outputs
       ```

       `--delete-conflicting-outputs` 标志会自动处理任何已有的冲突生成文件，确保干净生成。

    2. 如果 build_runner 失败，检查 pubspec.yaml 中 dev_dependencies 是否包含：
       - `build_runner: ^2.4.0`
       - `riverpod_generator: ^3.0.0`（或 ^2.x，匹配 riverpod_annotation 版本）
       - `json_serializable: ^6.0.0`
       如有缺失则先 `flutter pub add --dev {package}` 再重试。

    3. 生成完成后确认关键文件存在。
  </action>

  <verify>
    <automated>ls -la lib/features/import/providers/import_notifier.g.dart && ls -la lib/features/import/parsing/parse_candidate.g.dart && flutter analyze lib/features/import/ 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - 文件 `lib/features/import/providers/import_notifier.g.dart` 存在且非空（wc -l > 5）
    - 文件 `lib/features/import/parsing/parse_candidate.g.dart` 存在且非空（wc -l > 5）
    - `flutter analyze lib/features/import/` 返回 "No issues found!"（零错误、零警告）
  </acceptance_criteria>

  <done>import_notifier.g.dart 和 parse_candidate.g.dart 已生成，flutter analyze 零错误通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

本计划不引入新信任边界——仅运行标准 Dart 代码生成工具。

## STRIDE Threat Register

无新威胁。build_runner 是标准 Dart SDK 工具链的一部分，`--delete-conflicting-outputs` 仅影响生成文件（.g.dart），不影响手写源码。
</threat_model>

<verification>
- [ ] `flutter analyze` 对整个项目零错误通过
- [ ] `import_notifier.g.dart` 文件存在且包含 `ImportNotifier` 类生成代码
- [ ] `parse_candidate.g.dart` 文件存在且包含 `_$ParseCandidateFromJson` 函数
</verification>

<success_criteria>
1. 所有 `.g.dart` 文件已生成，项目恢复可编译状态
2. `flutter analyze` 零错误通过——Phase 2 代码无编译问题
3. 后续 Plan 02-02 和 02-03 的修复可在此基础上正常进行
</success_criteria>

<output>
After completion, create `.planning/phases/02-desktop-file-import-pipeline/02-01-SUMMARY.md`
</output>
