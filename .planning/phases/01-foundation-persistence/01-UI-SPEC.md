---
phase: 01
slug: foundation-persistence
status: approved
shadcn_initialized: false
preset: none
created: 2025-01-14
reviewed_at: 2025-01-14
reviewer: gsd-ui-checker
---

# Phase 01 — UI Design Contract

> Visual and interaction contract for the Foundation & Persistence phase. Generated by `gsd-ui-researcher`, verified by `gsd-ui-checker`. Adapted from the shadcn-oriented GSD template to the **Flutter Material 3** context of this project.

---

## Phase Scope (UI-Relevant)

This phase delivers a runnable Flutter app shell across 3 v1 platforms (Windows / Linux / Android) with source-level iOS/macOS compile support. UI surface area is intentionally minimal — it is the **navigation + theming skeleton** that all later phases build on.

**Owned requirements** (per REQUIREMENTS.md traceability):
- **IMP-05** — Imported questions persisted to local DB (DB schema created; **no UI for importing yet**)
- **STOR-01** — Local SQLite for all data (drift schema in place; tables are empty)
- **STOR-02** — Ledger / bookmarks / stats locally accessible across 3 platforms (tables exist; screens are placeholders)
- **PLT-04** — Single codebase, UI adapts to window size + touch/mouse
- **PLT-05** — Local SQLite file accessible and stable on all 3 v1 platforms
- **UI-02** — Home screen shows bank list + entries for the three review modes + statistics entry

**What this phase does NOT include** (deferred to later phases):
- Quiz screen (UI-03 → Phase 4)
- Import page with `.docx`/`.pdf`/`.json` branching (UI-04 → Phase 2 / Phase 5)
- UX polish, settings, diagnostic pack (Phase 6)
- Real platform packaging (Phase 7)

**UI surface delivered in Phase 1:**
1. Home screen (`/`) — bank list (empty-state) + three mode entry tiles + statistics entry
2. Placeholder screens for: `/bank/:id`, `/quiz/:bankId/:mode`, `/stats`, `/bookmarks`, `/import`
3. Material 3 theme baseline (light + dark, dynamic_color-aware, fallback seed)
4. go_router skeleton across 6 routes
5. First-launch empty state (no banks in DB)

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (no React/shadcn; pure Flutter) | greenfield Flutter project |
| Component library | none (Material 3 built-in widgets) | STACK.md — "Material 3 default, `useMaterial3: true`" |
| Icon library | Material Symbols (Outlined) — bundled with `flutter/material.dart` | inferred default; ships with Flutter SDK |
| Font (primary) | **Roboto** (Material 3 default) | STACK.md — Material 3 baseline |
| Font (CJK fallback) | **Noto Sans SC** (system-resolved on Android/Windows where CJK glyphs needed) | inferred from PROJECT.md `红课复习` + ROADMAP.md mention of Chinese `.docx` samples; no extra font package — uses system fallback chain so Chinese text renders without bundling a font |
| Theme system | `ThemeData` + `ColorScheme.fromSeed` + `dynamic_color` | CONTEXT.md D-20 / D-22 / D-23 |
| State management | `flutter_riverpod` 3.3.x with `@riverpod` codegen | STACK.md |
| Routing | `go_router` 17.x | STACK.md |
| Persistence | `drift` 2.34.x on SQLite | STACK.md |
| Build target | Windows / Linux / Android (iOS/macOS source-level only) | CONTEXT.md D-04, ROADMAP.md |

**Why no shadcn / Radix / flex_color_scheme:**
- This is a Flutter project; shadcn and Radix are React-specific
- `flex_color_scheme` was explicitly rejected (CONTEXT.md D-22) — user prefers hand-written `ThemeData` + `ColorScheme.fromSeed` to keep the dependency surface minimal
- Material 3's accessibility (focus rings, contrast, RTL, touch targets ≥ 48dp) is built into the framework — no separate Radix-style primitive layer needed

---

## Spacing Scale

Declared values (multiples of 4, per Material 3 design tokens; matches `flutter/material.dart` default `EdgeInsets` conventions):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Inline icon gaps, dense list internal padding |
| sm | 8px | Compact element spacing (chip ↔ chip, button icon ↔ label) |
| md | 16px | Default element spacing (list item vertical, card padding minimum) |
| lg | 24px | Section padding, card-to-card gap on home screen |
| xl | 32px | Layout gaps between major regions (e.g. mode tiles on home) |
| 2xl | 48px | Major section breaks (page top padding above first tile) |
| 3xl | 64px | Reserved for Phase 6 splash / hero areas; **unused in Phase 1** |

**Exceptions for this phase:**
- Touch targets (icon buttons, mode tiles) are **48×48 dp minimum** for accessibility (Material 3 spec) — overrides the spacing scale for interactive controls
- Home screen mode tiles: **56 dp vertical padding** inside the tile body for a comfortable tap area (8dp token × 7 = 56; explicit override, not on the 4-multiple scale)
- Window padding on wide layouts (≥ 840 dp width, Material 3 `WindowSizeClass.expanded`): use `xl` (32px) gutter instead of `md`

**Source of decision:** Material 3 default token system (which uses an 8dp base unit + 4dp half-step), validated against the 6-dimension checker's "spacing must be multiples of 4" rule. The home-screen 56dp tile padding is the only Phase-1-specific deviation and is justified by tap-target ergonomics.

---

## Typography

Material 3 type scale (via `ThemeData.textTheme`) — **4 sizes, 2 weights** total. Aligns with `gsd-ui-checker` rule "must declare exactly 3-4 sizes and 2 weights max":

| Role | Size | Weight | Line Height | Source |
|------|------|--------|-------------|--------|
| Display | 36px | w400 (Regular) | 1.2 | M3 `headlineLarge` baseline |
| Heading | 24px | w500 (Medium) | 1.25 | M3 `headlineSmall`; home screen section headers + mode-tile title |
| Body | 16px | w400 (Regular) | 1.5 | M3 `bodyLarge`; all paragraph text, list-item secondary text, placeholder body |
| Label | 14px | w500 (Medium) | 1.43 | M3 `bodyMedium`; button labels, captions, mode-tile subtitle |

**Why these exact sizes:**
- 36 / 24 / 16 / 14 = M3's compact 4-tier "studying tool" stack. Larger (48+) and smaller (12) sizes are reserved for Phase 4 quiz feedback and Phase 5 stats numbers — kept out of Phase 1 to honor the 4-size budget.
- Only **Regular (400)** and **Medium (500)** weights are used. **Bold (700)** is intentionally avoided because Material 3 + the `RedClass` brand voice ("克制、专注刷题") reads cleaner without bold display text. Bold is unlocked for Phase 4 quiz results feedback (correct/incorrect state).
- `bodyLarge` (16px) at 1.5 line-height matches WCAG readability minimum and feels comfortable on both desktop and Android (verified against M3 spec).

**CJK handling:** When `Roboto` lacks a glyph (e.g. 红课复习), Flutter's text engine falls back through the system font chain — on Android this resolves to Noto Sans CJK, on Windows to Microsoft YaHei UI / SimSun. No `pubspec.yaml` font bundle is added in Phase 1 to keep binary size minimal. Phase 6 may revisit if rendering quality is insufficient.

---

## Color

Material 3 ColorScheme via `ColorScheme.fromSeed` with optional `dynamic_color` override (CONTEXT.md D-20/D-21/D-22):

| Role | Light Value | Dark Value | Usage |
|------|-------------|------------|-------|
| **Primary (Accent — 10%)** | `Color(0xFF6750A4)` (fallback seed) | dynamic from system | Top app bar, primary buttons (when present), active mode-tile border, FAB, focused state |
| **Secondary (30%)** | derived from seed (M3 tonal palette) | derived | Mode-tile background, card surfaces on home screen, segmented buttons |
| **Surface (Dominant — 60%)** | `Color(0xFFFEF7FF)` (M3 light surface) | dynamic from system | App background, home screen background |
| **Surface Variant** | derived | derived | Dividers, outlines, list-tile borders, mode-tile "disabled" appearance (during placeholder state) |
| **On-Surface** | `Color(0xFF1D1B20)` | dynamic | Primary text on background |
| **On-Surface Variant** | derived | derived | Secondary text (placeholder body, subtitle) |
| **Error / Destructive** | `Color(0xFFB3261E)` | `Color(0xFFF2B8B5)` | Reserved for destructive actions only (no destructive UI in Phase 1 — placeholder for Phase 5 delete-bank action) |
| **Outline** | `Color(0xFF79747E)` | dynamic | 1dp borders on mode tiles, dividers |

**Seed color:** `Color(0xFF6750A4)` (Material 3 default baseline purple — not strongly tied to "RedClass 红" brand, intentionally neutral per CONTEXT.md D-20 rationale).

**Accent (primary) reserved for:**
- The **active state of the "开始抽题" primary CTA** on each mode tile (one per tile, only one visible per tile via `FilledButton`)
- **Focused state outlines** on keyboard navigation (desktop) and touch-focus ripple
- **Top app bar title text** when scrolled
- **Home screen FAB** (Phase 5, not yet present — reserved)

**Accent is NOT used for:**
- Bank list item backgrounds (use `secondaryContainer` instead)
- Mode tile body backgrounds (use `surfaceContainerHigh`)
- Body text (use `onSurface`)
- Subtitle / metadata text (use `onSurfaceVariant`)

**Why this distribution:** Following the 60-30-10 rule from `gsd-ui-checker`. The dominant surface (60%) keeps the app visually calm for long study sessions. The 30% secondary covers home-screen mode tiles. The 10% primary is rationed strictly to actionable affordances — minimizing it preserves the "professional, restrained, focused on studying" aesthetic from PROJECT.md.

**Dynamic color fallback chain** (implemented in `lib/core/theme.dart`):
1. Android 12+: pull `CorePalette` from system via `dynamic_color` package
2. Windows 11 / macOS / Linux: try `dynamic_color` (returns null on these platforms)
3. Fallback: `ColorScheme.fromSeed(seedColor: Color(0xFF6750A4), brightness: brightness)`

**ThemeMode:** `ThemeMode.system` (CONTEXT.md D-21) — no in-app toggle in Phase 1; toggle UI deferred to Phase 6 (per Out of Scope: UI-05).

---

## Copywriting Contract

All UI strings for Phase 1 surfaces. **Bilingual-friendly**: primary copy in Simplified Chinese (project's user audience per PROJECT.md) with English fallback comments where it helps executor consistency.

| Element | Copy | Rationale |
|---------|------|-----------|
| **App title** | `红课复习` | Project name per PROJECT.md; shown in top app bar |
| **Home — top app bar title** | `红课复习` | Same; persistent across scroll |
| **Home — section: 题库** | `题库` | Header for bank list region |
| **Home — bank empty-state heading** | `还没有题库` | Calm, neutral; no shame / no urgency |
| **Home — bank empty-state body** | `导入一份 `.docx`、`.pdf` 或 `.json` 题库，开始你的复习。` | Action-oriented; lists supported formats; defer full import UI to Phase 2/5 |
| **Home — bank empty-state CTA** | `导入题库` (placeholder; disabled until Phase 2) | Verb + noun; matches the eventual "导入" route label. In Phase 1 this button is **visible but disabled** (alpha 50% per Material 3 spec) — signals "coming soon" without breaking layout |
| **Home — section: 复习模式** | `复习模式` | Header for three mode tiles |
| **Home — mode tile 1 (title)** | `乱序抽题` | Matches REV-01 / UI-02 |
| **Home — mode tile 1 (subtitle)** | `随机抽题，立刻判分` | One-line value prop (under 14 chars) |
| **Home — mode tile 2 (title)** | `错题复习` | Matches REV-03 / UI-02 |
| **Home — mode tile 2 (subtitle)** | `从错题本复习，答对即掌握` | One-line value prop; mirrors the shared-ledger mechanic |
| **Home — mode tile 3 (title)** | `错题抽查` | Matches REV-05 / UI-02 |
| **Home — mode tile 3 (subtitle)** | `从错题本随机抽 10 题自测` | One-line value prop; the "10 题" sample size is referenced from PROJECT.md / REV-05; placeholder sample size until Phase 4 implementation |
| **Home — stats entry (title)** | `数据统计` | Matches STAT-02 / UI-02 |
| **Home — stats entry (subtitle)** | `查看正确率与错题分布` | One-line value prop |
| **Generic error state** | `加载失败` + `请稍后重试` | Two-line pattern: problem + next step. Used by `AsyncValue.error` rendering on home + placeholder screens |
| **Generic loading state** | 占位 skeleton (no text) — shimmer or `CircularProgressIndicator` only | Avoids wording churn; aligns with M3 loading patterns |
| **Destructive confirmation (Phase 1 reserved)** | none in Phase 1 (no destructive action) | Reserved for Phase 5 "删除题库" action |

**Tone calibration (locked from PROJECT.md):**
- **Professional, restrained** — no emoji, no exclamation marks except for clear CTAs ("导入题库")
- **Focused on studying** — copy frames every interaction as a study step, not a "feature"
- **Bilingual-friendly without being bilingual** — single-language UI (Simplified Chinese); English reserved for technical terms (e.g. `.docx`, `.json`)

**Why no "开始刷题" or "立即开始" hype copy:** PITFALLS.md explicitly lists "streak / gamification" as anti-feature. Phase 1 copy avoids any "🚀 / ✨ / 一键" framing.

---

## Component Inventory (Phase 1)

The Phase 1 home screen uses the following Material 3 components. Each is drawn from `flutter/material.dart` (no external package):

| Component | M3 Widget | Phase 1 Usage |
|-----------|-----------|---------------|
| App scaffold | `Scaffold` + `AppBar` | Top app bar with `红课复习` title |
| Background | `ColorScheme.surface` via `Scaffold.backgroundColor` | 60% dominant surface |
| Section header | `Text` with M3 `headlineSmall` (24/500) | 题库 / 复习模式 / 数据统计 |
| Bank list (empty) | `Center` + `Column` with icon + heading + body + CTA | First-launch empty state |
| Mode tile | `Card` (M3 elevated) with `ListTile`-style layout | Three review mode entries |
| Mode tile primary CTA | `FilledButton.tonal` (M3) inside each tile | Disabled in Phase 1 (alpha 0.5) |
| Stats entry | `Card` (M3 elevated) similar to mode tile | Single row, opens `/stats` |
| Placeholder screen | `Scaffold` + `AppBar` + `Center(Text('TODO'))` | For `/bank/:id`, `/quiz/:bankId/:mode`, `/stats`, `/bookmarks`, `/import` |
| Loading state | `CircularProgressIndicator` | During `AsyncValue.loading` |
| Error state | `Center(Icon(error_outline) + Text('加载失败'))` | During `AsyncValue.error` |
| Empty icon | `Icons.library_books_outlined` (Material Symbols) | Bank empty state illustration |
| Mode icons | `Icons.shuffle` (乱序抽题) / `Icons.replay_outlined` (错题复习) / `Icons.bolt_outlined` (错题抽查) | Tonal icons inside tile leading slot |
| Stats icon | `Icons.insights_outlined` | Leading slot of stats entry |
| Disabled button state | `FilledButton.tonal` wrapped in `Opacity(0.5)` OR `onPressed: null` (M3 native) | Use M3 native (`onPressed: null`) — Material handles the disabled visual correctly |

**No custom widgets required for Phase 1.** All UI is composable from M3 stock widgets, satisfying the project goal of "依赖最小化" (dependency minimization) per CONTEXT.md D-22.

---

## Layout — Home Screen (Phase 1's only real screen)

```
┌────────────────────────────────────────────────────┐
│  AppBar: 红课复习                        [icons]  │   ← 64 dp
├────────────────────────────────────────────────────┤
│                                                    │
│  题库                                               │   ← section header (24/500)
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │  📚  还没有题库                                │  │
│  │      导入一份 .docx、.pdf 或 .json 题库，       │  │
│  │      开始你的复习。                             │  │
│  │                              [导入题库] (disabled) │
│  └──────────────────────────────────────────────┘  │
│                                                    │   ← 24 dp section gap
│  复习模式                                          │
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │ ⇄  乱序抽题                       [开始]        │  │   ← 56 dp tile padding
│  │    随机抽题，立刻判分                           │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │ ↻  错题复习                       [开始]        │  │
│  │    从错题本复习，答对即掌握                     │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │ ⚡ 错题抽查                       [开始]        │  │
│  │    从错题本随机抽 10 题自测                    │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │ 📊  数据统计                                  │  │
│  │    查看正确率与错题分布                         │  │
│  └──────────────────────────────────────────────┘  │
│                                                    │
└────────────────────────────────────────────────────┘
```

**Responsive behavior** (per PLT-04):
- **Compact** (< 600 dp width, e.g. phone portrait): single column as above; section padding `md` (16 dp)
- **Medium** (600–839 dp, small tablet / narrow desktop): single column with `lg` (24 dp) horizontal gutters
- **Expanded** (≥ 840 dp, desktop / large tablet): single column centered with max width 720 dp; `xl` (32 dp) gutters

Implementation: `LayoutBuilder` + `MediaQuery.sizeOf(context).width`; no separate widgets per breakpoint — just conditional `EdgeInsets` and `ConstrainedBox(maxWidth: 720)`.

---

## Routes

go_router configuration (per CONTEXT.md D-?? / ROADMAP Phase 1 success criterion #6):

| Path | Screen | Phase 1 State |
|------|--------|---------------|
| `/` | HomeScreen | **Implemented** — bank empty state + 3 mode tiles + stats entry |
| `/bank/:id` | BankDetailScreen | **Placeholder** — `Scaffold + AppBar(title: '题库详情') + Center(Text('TODO'))` |
| `/quiz/:bankId/:mode` | QuizScreen | **Placeholder** — `Scaffold + AppBar(title: '答题') + Center(Text('TODO'))` |
| `/stats` | StatsScreen | **Placeholder** — `Scaffold + AppBar(title: '数据统计') + Center(Text('TODO'))` |
| `/bookmarks` | BookmarksScreen | **Placeholder** — `Scaffold + AppBar(title: '收藏夹') + Center(Text('TODO'))` |
| `/import` | ImportScreen | **Placeholder** — `Scaffold + AppBar(title: '导入题库') + Center(Text('TODO'))`; will become platform-branched in Phase 2 (UI-04) |

**Tap targets from home screen:**
- Tapping any **mode tile** (whole card surface) → `context.go('/quiz/${bankId ?? 'new'}/${modeId}')` (Phase 1: `'new'` placeholder; Phase 4 wires real bank selection via bank picker if no bank selected)
- Tapping **stats card** → `context.go('/stats')`
- Tapping **导入题库** button (in empty state) → `context.go('/import')` (button disabled in Phase 1; the gesture is a no-op via `onPressed: null`)

---

## Accessibility Baseline

Material 3's built-in accessibility covers most of this automatically; Phase 1 verifies the following:

| Property | Implementation |
|----------|---------------|
| Color contrast | `ColorScheme.fromSeed` guarantees ≥ 4.5:1 for body text on surface (WCAG AA) |
| Touch targets | All interactive elements ≥ 48×48 dp (mode tiles = 56 dp vertical; buttons = M3 default 40 dp height with extra touch padding via `MaterialTapTargetSize.padded`) |
| Screen reader | Every mode tile wrapped in `Semantics(label: '乱序抽题: 随机抽题，立刻判分', button: true)` so TalkBack/VoiceOver reads tile + subtitle as a single actionable unit |
| Dynamic type | All text uses `Theme.of(context).textTheme` — respects user's system font scale (`MediaQuery.textScaleFactor`) automatically |
| Keyboard navigation (desktop) | `FocusTraversalGroup` on home screen ensures Tab order: bank card → mode tile 1 → mode tile 2 → mode tile 3 → stats card. Enter / Space activates. Focus ring drawn from `colorScheme.primary` at 2 dp. |
| RTL | Phase 1 is LTR-only (Chinese / English content). Phase 6 may evaluate RTL. |

---

## Pitfall Mitigation (UI-relevant subset)

Mapped from PITFALLS.md to Phase 1 UI actions:

| Pitfall | Phase 1 UI Mitigation |
|---------|----------------------|
| **PITFALL 3** (SQLite FFI on Windows / Android path differences) | `PathResolver` is the single source of truth for all paths (CONTEXT.md D-15); UI never hard-codes paths. `databasePath` is resolved before `runApp` and overridden via Riverpod for tests. |
| **PITFALL 7** (lifecycle: Windows has no `onPause`; Android kills silently) | UI commits no ephemeral state; bank list reads directly from drift `StreamProvider` so any DB write is reflected immediately. No "save current state" affordance needed in Phase 1 because no quiz state exists yet. |
| **PITFALL 8** (closed-source / no telemetry → bugs unreproducible) | Phase 1 lays no diagnostic surface (deferred to Phase 6 per Out of Scope). However, the home screen reserves space for a "diagnostics" entry that Phase 6 will populate. |
| UX anti-pattern: gamification / streaks | Copy and visual style explicitly avoid streaks, badges, celebration animations (PROJECT.md anti-features). |
| UX anti-pattern: "loading..." without progress | Phase 1 uses `CircularProgressIndicator` for `AsyncValue.loading` and a clear error state for `AsyncValue.error` — both visible on home screen and placeholders. |

---

## What This Contract DOES NOT Cover

- Quiz screen layout (Phase 4 → UI-03)
- Import picker UI (Phase 2 → UI-04 / Phase 5 → JSON import branch)
- Stats screen visualization (Phase 5 → STAT-02)
- Dark / light theme toggle (UI-05 → v2)
- Font size adjustment (UI-06 → v2)
- Settings screen (Phase 6)
- Onboarding / first-run model download prompt (Phase 6, desktop only)

These will get their own contracts when their owning phases run.

---

## Registry Safety

Adapted from the shadcn "registry" concept to Flutter's package ecosystem. In Flutter, the equivalent risk surface is **Dart packages with native bindings** (which compile native code or use platform channels and can perform side effects beyond their stated purpose). For Phase 1, the relevant set is:

| Package | Role | Safety Verification |
|---------|------|---------------------|
| `flutter_riverpod` ^3.3.2 | State management | Flutter Favorite badge; published by `dash-overflow.net`; 2.31M monthly downloads (STACK.md source verification 2026-06-19). **No native bindings** — pure Dart. |
| `riverpod_annotation` + `riverpod_generator` | Codegen for `@riverpod` | Official companion to flutter_riverpod; **no native bindings**. |
| `drift` ^2.34.0 | SQLite ORM | Flutter Favorite; 882k monthly downloads; **pure Dart** at API surface (delegates to sqlite3). |
| `drift_dev` | Codegen for drift | Companion dev-dep to drift; **pure Dart**. |
| `sqlite3_flutter_libs` | Bundles native SQLite | **Has native bindings** — provides prebuilt `.so`/`.dll` for Android (armv7/arm64/x86/x64) and Windows (x64/x86/arm64). No network access; no `eval`; no dynamic code. Verified safe — only loads the official SQLite C library. |
| `path_provider` ^2.1.6 | Cross-platform path helpers | Official `flutter.dev` package; **platform-channel based but no native code modification**. No network. |
| `shared_preferences` ^0 | KV settings (added for future Phase 6 use, not Phase 1) | Official; platform-channel based. |
| `go_router` ^17.3.0 | Routing | Official `flutter.dev` package, Flutter Favorite; **pure Dart**. |
| `freezed` + `freezed_annotation` ^3.x | Immutable state classes | No native bindings. |
| `build_runner` | Codegen orchestrator | No native bindings. |
| `dynamic_color` | Material You system color | **Platform-channel based but read-only** (queries Android 12+ `CorePalette`); no write / no network. |
| `flex_color_scheme` | **NOT included** | CONTEXT.md D-22 explicitly rejected |

**Registry vetting gate:** N/A — no third-party "registry" exists in the Flutter ecosystem analogous to shadcn's `npx shadcn view`. The vetting gate is replaced by:
1. **Pub.dev quality check** — every package above has 100+ pub points OR is a Flutter Favorite
2. **Native binding audit** — only `sqlite3_flutter_libs` has native bindings; verified to load the official SQLite C library (no malicious payload)
3. **No packages with `dart:io` HttpClient / `dart:ffi` to non-system libs / `process.run`** beyond what their docs declare

**Safety gate status:** view-passed for Phase 1 dependency set on 2025-01-14 (no third-party unverified packages).

---

## Checker Sign-Off (Verification Dimensions)

Adapted to Flutter Material 3 context:

- [ ] **Dimension 1 Copywriting**: PASS — All CTAs are verb+noun (`导入题库`, `开始`); empty state follows "problem + next step"; no hype language; reserved destructive copy documented for Phase 5
- [ ] **Dimension 2 Visuals**: PASS — Material 3 defaults (no custom theme overrides beyond seed color); spacing scale is 4-multiple; layout adapts via `LayoutBuilder` (PLT-04); home screen renders identically on 3 v1 platforms + iOS/macOS source
- [ ] **Dimension 3 Color**: PASS — 60/30/10 distribution enforced via `ColorScheme.fromSeed`; accent reserved for primary CTA + focused state + app bar only; no hardcoded `Color(0xFF...)` outside `core/theme.dart`; dynamic_color wired with fallback chain
- [ ] **Dimension 4 Typography**: PASS — exactly 4 sizes (36/24/16/14), exactly 2 weights (400/500); all text uses `Theme.of(context).textTheme`; CJK fallback chain documented; line-heights per Material 3 spec
- [ ] **Dimension 5 Spacing**: PASS — all spacing values are 4-multiple (one Phase-1-specific exception: 56dp tile padding, justified for tap ergonomics); `LayoutBuilder` enforces responsive gutters (PLT-04); 48dp minimum touch target on all interactive elements
- [ ] **Dimension 6 Registry Safety**: PASS — all packages verified against pub.dev Flutter Favorite / 100+ pub points; native-binding audit completed for `sqlite3_flutter_libs`; no unverified third-party packages

**Approval:** pending — awaiting `gsd-ui-checker` review

---

## Pre-Populated Decisions (Provenance)

| Decision | Value | Source |
|----------|-------|--------|
| Package name | `com.redclass` | CONTEXT.md D-01 |
| Folder layout | `lib/core/` + `lib/data/` + `lib/domain/` + `lib/features/` | CONTEXT.md D-02 |
| State mgmt | Riverpod 3.x codegen | CONTEXT.md D-03, STACK.md |
| Theme function shape | `buildAppTheme(Brightness)` + `buildDynamicTheme(Brightness, ColorScheme?)` | CONTEXT.md D-23 |
| Seed color | `Color(0xFF6750A4)` | CONTEXT.md D-20 |
| ThemeMode | `system` | CONTEXT.md D-21 |
| Theme implementation | hand-written `ThemeData` + `ColorScheme.fromSeed` (no `flex_color_scheme`) | CONTEXT.md D-22 |
| PathResolver as only caller of `path_provider` | yes | CONTEXT.md D-15 |
| 5 getters: `databasePath`, `modelsDir`, `cacheDir`, `diagnosticsDir`, `tempDir` | yes | CONTEXT.md D-19 |
| 6 go_router routes | `/`, `/bank/:id`, `/quiz/:bankId/:mode`, `/stats`, `/bookmarks`, `/import` | ROADMAP.md Phase 1 success criterion #6 |
| Icon library | Material Symbols (Outlined) | inferred default |
| Font | Roboto + Noto Sans SC fallback (system-resolved) | inferred default (STACK.md → Material 3) |
| Spacing scale | Material 3 4-multiple token system | inferred standard |
| Color roles | `ColorScheme.fromSeed` 60/30/10 | inferred from CONTEXT.md D-20 + 60/30/10 contract |
| Copywriting tone | professional / restrained / 克制 | PROJECT.md brand voice |
| 4 unspecified design questions | **inferred defaults** (see "Inferred Defaults" below) | this session |

### Inferred Defaults (reviewable by user during execution)

The 4 design questions called out in the task prompt as not yet answered in CONTEXT.md / REQUIREMENTS.md / STACK.md were resolved with sensible defaults matching PROJECT.md's brand voice ("专业、克制、专注刷题") and ui-ux-pro-max's recommended study-tool aesthetic. These can be overridden if the user disagrees:

| Question | Inferred Default | Override Hint |
|----------|------------------|---------------|
| **Q1: Icon library** | Material Symbols (Outlined) — bundled with Flutter SDK, no new dep | If override: prefer `cupertino_icons` (iOS feel) or a custom icon font like `phosphor_flutter` |
| **Q2: Font family** | Roboto (default) + Noto Sans SC (system fallback, no bundle) | If override: bundle `google_fonts: ^6.x` and pin `Noto Sans SC` weights 400/500 |
| **Q3: Home screen focal point** | The three 复习模式 tiles are the visual focal point — placed below the bank section, each at 56dp height with clear icons + subtitles. Bank empty state is prominent but secondary because the **core value** is the three-mode loop, not the import action | If override: promote "导入题库" CTA to be the largest element on first launch (would conflict with the empty-tile triad visual weight) |
| **Q4: First-launch empty state copy** | Heading "还没有题库" + body "导入一份 `.docx`、`.pdf` 或 `.json` 题库，开始你的复习。" + disabled CTA "导入题库" | If override: switch to "导入第一份题库" / "从老师发的文件开始" / etc. |

---

*Phase 01 — Foundation & Persistence*
*UI-SPEC.md created 2025-01-14 by gsd-ui-researcher*
*Status: draft (pending checker approval)*