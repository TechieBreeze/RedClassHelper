---
phase: 05-json-export-import-multiple-choice-bookmarks-statistics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/export/services/json_export_service.dart
  - test/features/export/json_export_service_test.dart
autonomous: true
requirements:
  - IMP-06

must_haves:
  truths:
    - "JsonExportService converts DB Questions to user JSON format with numbered objects, answer map, key string, answer_type 0/1"
    - "JsonExportService converts user JSON format back to DB entities (round-trip preserves all question data)"
    - "Questions are sorted numerically by their position in the bank (1, 2, 3...), not by raw key iteration order"
    - "Exported JSON metadata contains only name + version (no timestamps, no UUIDs per D-02)"
    - "Single-choice questions export as answer_type: 0, multi-choice as answer_type: 1"
  artifacts:
    - path: "lib/features/export/services/json_export_service.dart"
      provides: "Bidirectional JSON format conversion between DB schema and user JSON format"
      min_lines: 80
      exports: ["bankToUserJson", "userJsonToEntities"]
    - path: "test/features/export/json_export_service_test.dart"
      provides: "Unit tests for format conversion and round-trip integrity"
      min_lines: 80
  key_links:
    - from: "json_export_service.dart"
      to: "lib/data/db/tables/questions.dart"
      via: "imports Question, QuestionBank data classes"
      pattern: "import.*database"
    - from: "json_export_service.dart"
      to: "dart:convert"
      via: "jsonEncode/jsonDecode for options/correct JSON parsing"
      pattern: "jsonDecode|jsonEncode"
---

<objective>
Create the JSON export service -- a bidirectional converter between the drift DB schema
(optionsJson/correctJson/type columns) and the user's established JSON format (D-01: numbered
objects, `answer` option map, `key` concatenated string, `answer_type` 0/1).

Purpose: This is the data contract layer. Every other Phase 5 feature (export dialog, import
fast-track, round-trip verification) depends on this service producing and consuming the correct
format. Get the conversion right here, and the rest of Phase 5 is straightforward.

Output: `json_export_service.dart` with two public functions + comprehensive unit test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-json-export-import-multiple-choice-bookmarks-statistics/05-CONTEXT.md
@.planning/phases/05-json-export-import-multiple-choice-bookmarks-statistics/05-RESEARCH.md
@.planning/phases/05-json-export-import-multiple-choice-bookmarks-statistics/05-VALIDATION.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Key types the executor needs. Extracted from existing codebase. -->

From lib/data/db/tables/questions.dart:
```dart
@DataClassName('Question')
class Questions extends Table {
  TextColumn get id => text()();
  TextColumn get bankId => text().named('bank_id').references(QuestionBanks, #id, onDelete: KeyAction.cascade)();
  TextColumn get type => text()();           // 'single' | 'multiple'
  TextColumn get stem => text()();
  TextColumn get optionsJson => text().named('options_json')();  // '[{"key":"A","text":"..."}, ...]'
  TextColumn get correctJson => text().named('correct_json')();  // '["A"]' or '["A","C"]'
  TextColumn get rawText => text().named('raw_text')();
  DateTimeColumn get createdAt => dateTime()();
}
```

From lib/data/db/tables/question_banks.dart:
```dart
@DataClassName('QuestionBank')
class QuestionBanks extends Table {
  TextColumn get id => text()();
  TextColumn get name => text()();
  TextColumn get source => text()();
  IntColumn get questionCount => integer().named('question_count')();
  DateTimeColumn get createdAt => dateTime()();
  DateTimeColumn get updatedAt => dateTime()();
}
```

User JSON format (D-01):
```json
{
  "name": "2024秋-数据库原理",
  "version": "1.0",
  "questions": {
    "1": {
      "question": "1. 数据库系统的核心是_____。",
      "answer": {"A": "数据库", "B": "数据库管理系统", "C": "数据模型", "D": "软件工具"},
      "key": "B",
      "answer_type": 0
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create JSON export service (TDD: test first, then implement)</name>
  <files>lib/features/export/services/json_export_service.dart, test/features/export/json_export_service_test.dart</files>

  <read_first>
    - lib/data/db/tables/questions.dart — MUST read to understand Question data class fields (type, optionsJson, correctJson, stem)
    - lib/data/db/tables/question_banks.dart — MUST read to understand QuestionBank data class fields (name)
    - .planning/phases/05-json-export-import-multiple-choice-bookmarks-statistics/05-RESEARCH.md — Section "Pattern 1: JSON Format Conversion Layer" contains the exact conversion mapping table and code examples
    - .planning/codebase/CONVENTIONS.md — const constructors, @immutable, file organization conventions
  </read_first>

  <behavior>
    - Test 1: `bankToUserJson()` with single-choice question produces answer_type=0, key="A", answer={"A":"text1","B":"text2"}
    - Test 2: `bankToUserJson()` with multi-choice question produces answer_type=1, key="AC", answer map with 4 entries
    - Test 3: `bankToUserJson()` with empty question list produces `{"name":"X","version":"1.0","questions":{}}`
    - Test 4: `bankToUserJson()` with 5 questions produces numbered keys "1" through "5" (not "1","10","2"...)
    - Test 5: `bankToUserJson()` DOES NOT include timestamps or UUIDs (D-02: grep for createdAt, updatedAt, id in output fails)
    - Test 6: `userJsonToEntities()` parses valid JSON and returns correct QuestionsCompanion list with type='single', optionsJson, correctJson
    - Test 7: `userJsonToEntities()` with answer_type=1 produces type='multiple'
    - Test 8: `userJsonToEntities()` with key="ABC" produces correctJson '["A","B","C"]'
    - Test 9: Round-trip: bankToUserJson → userJsonToEntities → questions have same stems, keys, options
    - Test 10: `userJsonToEntities()` with non-numeric question keys like "Q1" throws FormatException
    - Test 11: `userJsonToEntities()` with missing 'questions' key throws FormatException
    - Test 12: `userJsonToEntities()` with key containing character outside A-H (e.g., "Z") throws FormatException
    - Test 13: `userJsonToEntities()` with answer_type=2 (invalid) throws FormatException
  </behavior>

  <action>
    Create two files:

    **FILE 1: `lib/features/export/services/json_export_service.dart`**

    Create the directory `lib/features/export/services/` if it does not exist.

    Implement two top-level functions (no class needed -- these are pure data transforms):

    1. `Map<String, dynamic> bankToUserJson(QuestionBank bank, List<Question> questions)`
       - Convert `bank.name` → output `"name"` field
       - Set `"version"` to the hardcoded string `"1.0"` (D-02: no dynamic version)
       - Iterate questions in given order; output key = `'${index + 1}'` (1-based string)
       - For each question:
         - Parse `q.optionsJson` with `jsonDecode` → `List<dynamic>`, each element is `{"key":"A","text":"..."}`
         - Build `Map<String, String> answer` = `{opt['key']: opt['text']}`
         - Parse `q.correctJson` with `jsonDecode` → `List<dynamic>`, join to string: `correctKeys.join()`
         - Set `"answer_type"` = `q.type == 'multiple' ? 1 : 0`
         - Set `"question"` = `'${index + 1}. ${q.stem}'` (prefix with question number + ". ")
         - Set `"key"` = concatenated correct keys string (e.g., `"B"` or `"ABC"`)
       - DO NOT include: timestamps (createdAt/updatedAt), UUIDs (id/bankId), or rawText (D-02)

    2. `({String bankName, List<QuestionsCompanion> questions}) userJsonToEntities(Map<String, dynamic> json, String bankId)`
       - Validate top-level structure: must have `name` (String, non-empty), `version` (String), `questions` (Map, non-empty)
       - Throw `FormatException('message')` with specific message on validation failure
       - Extract `questions` map; get keys, parse each to `int` (throw FormatException if non-numeric)
       - Sort keys numerically: `sortedKeys.sort()`
       - For each key:
         - Get question object `q`
         - Validate `q['key']` is String matching regex `^[A-H]+$`; throw FormatException if invalid
         - Validate `q['answer_type']` is int, 0 or 1; throw FormatException if invalid
         - Validate `q['answer']` is Map<String, String>; throw FormatException if invalid
         - Convert `answer` map to options list: `answerMap.entries.map((e) => {'key': e.key, 'text': e.value}).toList()`
         - Convert `key` string to list: `keyStr.split('').toList()` (one char per element)
         - Build `QuestionsCompanion.insert(...)` with:
           - `id: const Uuid().v4()`
           - `bankId: bankId`
           - `type: answerType == 1 ? 'multiple' : 'single'`
           - `stem: q['question']` (verbatim, includes number prefix per D-01)
           - `optionsJson: jsonEncode(optionsList)`
           - `correctJson: jsonEncode(correctList)`
           - `rawText: q['question']`
           - `createdAt: DateTime.now()`
       - Return `(bankName: json['name'], questions: companions)`

    Import `package:uuid/uuid.dart` (already in pubspec.yaml) and `dart:convert`.

    **FILE 2: `test/features/export/json_export_service_test.dart`**

    Write tests following the 13 behaviors listed above. Use `flutter_test` (`package:flutter_test/flutter_test.dart`).
    Import `package:redclass/data/db/database.dart` for QuestionBank and Question data classes.
    Use `AppDatabase.openInMemoryDatabase()` for any DB-backed tests (round-trip test 9).
    Import `package:drift/drift.dart` for QuestionsCompanion.

    Create helper factory functions to build test Question and QuestionBank instances
    without needing a real DB connection for unit-level format tests (tests 1-8, 10-13).
    For the round-trip test (test 9), insert into an in-memory DB, read back, then convert.

    Run RED phase: `flutter test test/features/export/json_export_service_test.dart` — MUST fail.
    Then implement the service. Run GREEN phase: same command MUST pass all 13 tests.
  </action>

  <verify>
    <automated>flutter test test/features/export/json_export_service_test.dart</automated>
  </verify>

  <acceptance_criteria>
    - `lib/features/export/services/json_export_service.dart` exists and exports `bankToUserJson` and `userJsonToEntities`
    - `test/features/export/json_export_service_test.dart` exists with 13 test cases
    - All 13 tests pass (command exits 0)
    - Output JSON from `bankToUserJson()` contains exactly keys: "name", "version", "questions" -- no "id", "createdAt", "updatedAt", "rawText", "source"
    - `userJsonToEntities()` throws FormatException for: non-numeric keys, missing questions field, invalid key chars (outside A-H), invalid answer_type
    - Round-trip test: export a bank → import back → same stems, keys, option texts
    - `dart analyze lib/features/export/` exits 0 with no errors
  </acceptance_criteria>

  <done>JSON export service with 13 passing tests, bidirectional conversion verified by round-trip test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON input → `userJsonToEntities()` | Untrusted JSON data crosses into domain entities |
| Domain entities → `bankToUserJson()` | Internal data crosses to user-readable file system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `userJsonToEntities()` key field | mitigate | Validate `key` contains only A-H characters via regex `^[A-H]+$` at parse entry; throw FormatException on reject |
| T-05-02 | Denial of Service | `userJsonToEntities()` oversized input | accept | JSON files >10MB rejected downstream in ImportNotifier before reaching converter; converter itself has no DoS surface beyond normal parsing |
| T-05-03 | Information Disclosure | `bankToUserJson()` output | mitigate | D-02: export only `name` + `version` metadata; do NOT include timestamps or UUIDs in output JSON; grep verification in test |
</threat_model>

<verification>
Run `flutter test test/features/export/json_export_service_test.dart` -- all 13 tests must pass.
Run `dart analyze lib/features/export/` -- must exit 0.
</verification>

<success_criteria>
- [ ] `json_export_service.dart` exists with correct bidirectional conversion
- [ ] 13 unit tests pass
- [ ] Round-trip preserves all question data (stems, keys, option texts)
- [ ] Export never leaks timestamps or UUIDs (D-02)
- [ ] Import validates key charset (A-H only), answer_type (0 or 1), numeric question numbering
- [ ] `dart analyze` clean on new code
</success_criteria>

<output>
After completion, create `.planning/phases/05-json-export-import-multiple-choice-bookmarks-statistics/05-01-SUMMARY.md`
</output>
